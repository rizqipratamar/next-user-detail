import { PageHeader } from "antd";

const HeaderLayout = () => {
  return (
    <PageHeader
      className="site-page-header"
      title={"List Users"}
      subTitle={"List User from mock API"}
    />
  );
};

export default HeaderLayout;
